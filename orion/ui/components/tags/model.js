// Todo: remove from here; mostly used in demo code
export class TagModel {
    constructor(options) {
        this.id = options.id;
        this.text = options.text;
        this.url = options.url;
    }
}